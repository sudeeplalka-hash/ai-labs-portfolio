// Intentionally empty, this component was removed during a redesign and is no
// longer used anywhere. The sandbox can't delete files on the mounted drive, so
// it's left as an empty stub. Safe to delete this file locally.
export {};
