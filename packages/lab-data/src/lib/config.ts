// Suite configuration. The Data Lab hands its approved corpus off to the RAG
// Evaluator — set this to wherever that lab is served (its dev server, its
// static export, or its production URL).
export const RAG_EVALUATOR_URL = "http://localhost:3000";
