# Overnight handoff — 2026-07-11 · Command Center refinement build

For the overnight session(s). Read `CLAUDE.md` first (Redundancy Gate + sandbox rituals), then
`docs/REFINEMENT-PLAN.md` (the full QA findings), then this file. Work the phases IN ORDER; every
phase must ship alone (gates green → commit via the GIT_INDEX_FILE plumbing → direct ref write).
DO NOT PUSH — leave commits local; Sudeep pushes in the morning and verification happens then.

## Phase R1 — Coherence (do first, engine-level)

### R1.1 One demo fixture, all widgets derive from it  ← the centerpiece
- Today: Govern's `GovSampleBanner` says "no initiative loaded" while `useProgram()` has the demo
  initiative; Deploy's `ReleaseBlockers` claims "Data lab not run" while the stepper shows Data 74.
- Find every place that seeds or checks demo state independently:
  `grep -rn "sample\|demo\|No live initiative\|not run" apps/web/components packages/program-core/src packages/lab-*/src --include="*.tsx" -l`
- Design: `packages/program-core/src` gains ONE exported demo fixture (or one `isDemoState(state)`
  + `demoGaps(state)` selector pair). `ReleaseBlockers`, `GovSampleBanner`, the stepper, and the
  handoff strips all read THAT. A blocker may only exist if the fixture actually lacks the artifact.
- Add a program-core test: "the shipped demo fixture produces zero contradictions" (no blocker may
  reference a stage whose fixture score is present; banner state must match initiative presence).
- Acceptance: on /deploy and /govern, banners/blockers/stepper tell one story.

### R1.2 Stage-04 naming, settled everywhere
- Decision (already made, apply it): 04 = "Deploy · AI Ops" (sub: "make it run"),
  07 = "Operate · Day Two" (sub: "keep it running").
- Touch: `packages/program-core` STAGES list (labels/subs), `DeployStage.tsx` PageIntro eyebrow
  ("STAGE 04 · OPERATE, AI OPS · MLOPS · LLMOPS" → "STAGE 04 · DEPLOY · AI OPS"), the /deploy
  page metadata title ("AI Ops · AI Command Center" → "Deploy · AI Ops · …"), OperateStage eyebrow
  if needed. Grep both old strings to catch stragglers.

### R1.3 Blocker rows route to their source stage
- `apps/web/components/operate/ReleaseBlockers.tsx`: rows currently all label "fix in Deploy".
  Each blocker gets a `sourceStage` (data/build/…) and the CTA reads "fix in Data →" linking there.

### R1.4 Shared formatters + Realize single-sourcing
- DONE in this commit: "1 months" pluralization (RealizeView.tsx).
- Remaining: `packages/engines` (or design-system lib) gains `formatMoney(n, {style})` — one style
  decision: $2.5M/yr above $999k, $281k below; use it in RealizeView verdict AND the Realize handoff
  strip (which today shows a DIFFERENT number: $2.4M vs $2,545k — find where the handoff strip
  computes its figure, `grep -rn "2.4M\|M/yr" packages apps` won't work, it's computed: trace
  `handoff.ts` in program-core). Both must read the same computed value.

## Phase R2 — Structure (presentation only)

### R2.1 One header per stage
- Merge the handoff card (the "0N · STAGE — question / metric / in→out" strip rendered above every
  stage) INTO `PageIntro` (design-system): PageIntro gains optional `inStage`/`outStage`/`metricLine`
  props; stage pages drop the separate strip. One question per page, asked once.
- Files: `packages/design-system/src/components/ui.tsx` (PageIntro), each `apps/web/components/stages/*.tsx`,
  and the component that renders the strip (grep "the full story" to find it).

### R2.2 One nav config, one StageSubnav
- Move the sidebar's SUBNAV structure (apps/web/components/shell/Sidebar.tsx) into
  `packages/kit/src/labs.ts` (export `STAGE_SECTIONS`), typed + grouped.
- Sidebar consumes it; NEW shared `StageSubnav` component (Govern's grouped visual pattern) replaces
  DataSubnav/BuildSubnav/GovSubnav; mounts in the three layouts + ADD Deploy/Realize layouts
  (their hash-section entries). Delete the three old *Subnav components after parity.
- Acceptance: Build's in-page subnav shows the SAME grouped sections as the sidebar (today it
  diverges: flat 10-row vs 4 groups incl. Agents/Training/Internals).

### R2.3 Stepper units + tooltips
- The component rendering "Strategy 75 › Data 74 › … › Realize 180% › Operate 4"
  (grep "release blockers" neighbor or trace from AppShell) gets per-stage `title` tooltips and
  unit-aware labels: "74/100", "ROI 180%", "4 signals". Keep it visually as-is otherwise.

### R2.4 Rail truncation
- "Strategy…"/"Build · R…": shorten display labels in STAGES ("Strategy", "Build · RAG" fits?
  test at current rail width) or widen the rail 16-24px.

## Phase R3 — Polish (only after R1+R2 are committed)
- "Live lab | Demo" pill + "Knowledge assistant": add `title` tooltips + a one-line first-run
  explainer under the pill; ensure "Demo" wording aligns with the SIMULATED badge doctrine.
- OperateStage: merge the Watch/Decide/Loop-back duplicate (cards + buttons) into one stepper.
- Copy pass with the new formatters ("$24k/month", "p95 1.40s", percent style) — grep raw "$" +
  "p95" in stage components; route through formatters.
- Leave mobile/a11y/empty-state audits for a daytime session (need interactive verification).

## Rituals (unchanged, see CLAUDE.md)
- /tmp rebuilds: `git archive HEAD | tar -x -C /tmp/wk`; tools = `npm i pnpm vitest typescript` in
  /tmp/tools; `pnpm install --frozen-lockfile --store-dir /tmp/pnpm-store` (3 rounds, resumable).
- Gates per change: package vitest + `tsc -p tsconfig.delta.json` (recreate: paths for @/, @data, @rag,
  @gov, @labs/*, typeRoots apps/web/node_modules/@types) + scoped eslint.
- Commit: reliable-write to mount (sha + NUL verify) → GIT_INDEX_FILE read-tree/add/write-tree/
  commit-tree → python-verify-and-write `.git/refs/heads/main`. NEVER trust mount `git status`.
- NO PUSHING overnight. Morning: Sudeep pushes; then stamp-verify + before/after screenshots.

## Definition of "extremely refined" (the bar)
A first-time reviewer can walk Frame → Operate without meeting a contradiction, a duplicated
question, an unlabeled number, or a control they can't parse. Every number formatted one way.
Every stage named one way. One navigation truth.

## Morning status (written 2026-07-11 morning, post-overnight + close-out session)

- Overnight run fired on schedule (1:15 AM) and delivered FAR more than its commit message says:
  `5db6d44` contains ALL of R1 **and all of R2** (StageThread folded into the header, kit
  STAGE_SECTIONS + shared StageSubnav wired in all five layouts, stepper units/tooltips via
  selectStageHeadlines, rail w-64 -> w-72). Gates at commit time: 478 tests across 8 packages,
  apps/web tsc clean. It ran out of runway before R3 and before writing this section.
- Close-out session completed R3: Header pill + archetype tooltips were already in (overnight);
  Operate's duplicated view switcher merged (the orientation cards ARE the switcher now);
  formatters verified engine-wide (format.ts + fixture-sync tests); dead DataSubnav/BuildSubnav/
  GovSubnav deleted from the tree.
- Repaired in passing: the mount worktree copy of OperateStage.tsx was truncated 100 bytes short
  of its committed blob (interrupted final write overnight) — rewritten from the HEAD blob.
- Morning checklist: push from Desktop (do NOT commit the phantom "changed files" residue) ->
  stamp-verify -> before/after screenshots of /deploy, /govern, /build, /realize, /operate.
- Remaining, deliberately deferred: mobile/responsive, a11y, and fresh-visitor empty-state audits
  (need interactive daytime verification).
