{
  "name": "@labs/engines",
  "version": "0.0.0",
  "private": true,
  "description": "Pure, framework-agnostic decision engines behind the labs, risk adjusted portfolio ROI, NPV/IRR/payback, protocol scoring, human in the loop review policy, and artifact formatting. No React, no DOM. Extracted so the exact formulas the UI shows are unit-tested in isolation.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "5.5.3",
    "vitest": "2.0.5"
  }
}
