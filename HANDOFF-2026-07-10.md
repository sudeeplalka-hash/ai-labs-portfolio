# Overnight run handoff — 2026-07-10 (Collection 5, Phases 0–5)

Read top to bottom before touching anything. Everything below is committed locally and UNPUSHED.

## 1. FIRST: why your deploys were failing (fixed, needs your push)

`0cad3ef` ("Corpus moved to 12 tests") accidentally **deleted `pnpm-lock.yaml`**. Without a lockfile Vercel
falls back to legacy pnpm 6, which crashes on modern Node (`ERR_INVALID_THIS` on every registry fetch) —
that is the exact error in the failed builds' logs. Your `ea9bf75` re-deleted my first restore because the
working tree never got the file back. **`d5dbae0` restores it in both the tree and the working folder.**
The live site never broke — it still serves `7be760c` (which already includes the 14-file corpus).

## 2. Commits to push (in order, all local on main)

- `d5dbae0` — pnpm-lock.yaml restored (THE deploy fix)
- `bf91c2e` — **LB-03 Model Evaluation & Threshold Economics** at `/builds/eval-bench`: logistic model
  trained live in-browser on a disclosed synthetic corpus; real ROC/PR/calibration/Brier + cost-vs-threshold
  U-curve with editable economics. 8 new engine tests (169 total across engines). Closes the "no ROC/AUC
  anywhere" gap your own docs rank #1.
- `cd33443` — LB-04 (NER) + LB-05 (inspection) build cards in `docs/live-builds/` (deps pinned, VisA over
  MVTec for license, acceptance criteria) + LB-01/LB-02 field notes in `docs/field-notes/`.
- `301f14f` — **LB-07 labs-catalog MCP server** registered (RECORDED badge, first use; legend gains the dot).

Earlier, also unpushed: `d021ba7`, `020abcb`, `6fbb73e`, `6c3c6cb`, `ac83563` (see HANDOFF-2026-07-09 + git log).

## 3. New standalone repo, built AND run tonight: `Projects/labs-catalog-mcp/`

A real MCP server over the portfolio registry (3 tools), plus `tee-wire.mjs` (30-line wire tap) and a
**captured real session** — `session-transcript.jsonl`, 11 JSON-RPC frames, regenerable with
`npm run capture`. Publish it exactly like the other two repos (GitHub Desktop → publish as
`labs-catalog-mcp`); LB-07's href already points at `github.com/sudeeplalka-hash/labs-catalog-mcp`.

## 4. Morning runbook (order matters)

1. GitHub Desktop: if it shows an uncommitted *deletion* of `pnpm-lock.yaml` — Discard it. Then **push**
   ai-labs-portfolio. Both Vercel projects should go READY; footer stamp should read `301f14f`.
2. Publish the three standalone repos (Desktop → Add local repository → publish):
   `enterprise-ai-governance-control-plane` (delete its broken `.git` folder first — sandbox left it),
   `RAG Evaluator` (as `rag-quality-evaluator`), `labs-catalog-mcp`.
3. vercel.com/new: import the first two as projects named exactly `ai-governance-control-plane` and
   `rag-quality-evaluator` (settings are pre-baked; governance reads its vercel.json).
4. Ask Claude to verify: live stamp, Collection 5 section (4 rows: LB-01/02/03/07), both deploy URLs,
   and fix registry hrefs if Vercel suffixed the names.

## 5. Deliberately NOT built tonight (and why)

- **LB-04 / LB-05 code**: both need a runtime model download verifiable only in a real browser; shipping
  untested LIVE claims breaks the collection's bar. Build cards are ready in `docs/live-builds/`.
- **LB-06 / LB-08**: need local model runtimes / API keys the sandbox doesn't have.
- **Governance split-screen hook** (Phase 1 second half): real UI work in a 26-page app I can't run;
  sketch: landing hero splits "ungoverned vs governed" on the same prompt — left raw mock answer, right the
  gateway verdict chain (guardrails hit → decision → audit entry). Its `ENHANCEMENT-RECOMMENDATIONS.md` has
  the rest.
- **Atlas technique tags** (cosmetic) — listed in COLLECTION5-LIVE-BUILDS-PLAN.md Phase 5.

## 5b. Built in the continuation run (after your "continue building" message)

- **`Projects/decision-engines/` — the npm package, publish-ready.** Name `ai-decision-engines` verified
  available on the registry; builds to ESM+types, real Node import smoke-tested (66 exports), 161/161 tests,
  `npm pack` validates at 58.5 kB. To publish: `cd decision-engines && npm i && npm login && npm publish`.
  Includes the new evalbench engines, so the package ships ROC/PR/calibration/threshold math too.
- **LB-04 verification attempt, honest result:** huggingface.co is unreachable from the sandbox (web_fetch
  times out), so the NER model could not be verified here and LB-04 correctly stays a build card — first
  session with a real browser: run the model per docs/live-builds/NEXT-LB04-LB05.md, then register it.

## 6. Sandbox facts the next session must know

- Mount corruption is intermittent: a fresh `git init` got a garbled `.git/config` (that's the broken `.git`
  in the governance folder); the reliable-write ritual (write → sha verify → NUL check) held for every file.
- Commits go through `GIT_INDEX_FILE` plumbing + a direct ref write (`update-ref` is blocked by phantom locks).
- `/tmp` was recycled mid-run: re-tar `/tmp/wk` from `git archive HEAD`, reinstall tools
  (`npm i pnpm vitest typescript` in `/tmp/tools`), `pnpm install` resumes from the store in ~3 rounds.
- Background processes get killed silently; run everything foreground with `timeout 40` + turbo cache,
  or per-package. Full `next build` is Vercel's job.

— end of handoff
